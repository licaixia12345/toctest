// JavaScript 文件 - js02.js

// 基本的 for 循环示例
for (let i = 0; i < 5; i++) {
  console.log(`第 ${i} 次循环`);
}

// 遍历数组的 for 循环
const numbers = [1, 2, 3, 4, 5];
for (let i = 0; i < numbers.length; i++) {
  console.log(`数字：${numbers[i]}`);
}
